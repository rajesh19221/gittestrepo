{
  "name": "my-nodejs-app",
  "version": "1.0.0",
  "description": "A sample Node.js app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Building the app...' && sudo mkdir -p /var/www/html && sudo rsync -av --progress --exclude=/var/www/html / /var/www/html/"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
